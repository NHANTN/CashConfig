# 收银台配置管理平台 v1.0.0

> 本文档由需求模板自动生成，生成时间：2026-05-27 10:53:23

---

## 1. 项目概述

| 项目 | 内容 |
|---|---|
| 项目名称 | 收银台配置管理平台 |
| 版本号 | 1.0.0 |
| 负责部门 | 技术部 / 产品部 |
| 项目描述 | 一个用于管理收银台配置的后台管理系统，支持多门店、多设备的配置统一管理 |

### 1.1 项目背景

随着门店数量增加，收银台配置分散管理导致效率低下， 需要一个统一的配置管理平台来集中管理所有收银终端的配置。

### 1.2 项目目标

- 统一管理所有门店收银台配置
- 支持配置的远程下发与实时更新
- 提供配置变更的审计与回滚能力
- 降低门店运维成本，提升配置效率

### 1.3 目标用户

- IT运维人员

---

## 2. 用户角色与权限

| 角色 | 描述 | 权限 |
|---|---|---|
| 超级管理员 | 拥有系统全部权限 | 系统全部功能 |
| 运营管理员 | 管理门店、设备、商品等核心配置 | 门店管理、设备管理、商品管理、支付配置、收银台布局配置、查看报表 |

---

## 3. 前端功能模块

### 登录与认证 — 🔴 核心（P0）

用户登录、权限校验、多设备登录管理

**功能列表：**

- 账号密码登录
- 验证码登录（可选）
- LDAP/OAuth 单点登录（可选）
- 登录态过期处理
- 权限路由动态加载

### 仪表盘 — 🔴 核心（P0）

首页概览，展示关键运营数据

**功能列表：**

- 今日交易总额
- 今日订单数
- 在线设备数
- 待处理告警
- 近7天交易趋势图

### 门店管理 — 🔴 核心（P0）

管理所有门店信息

**功能列表：**

- 门店列表（搜索/分页/筛选）
- 新增/编辑门店
- 门店启停用
- 门店详情（设备数量、员工数）
- 门店分组管理

### 设备管理 — 🔴 核心（P0）

管理收银台终端设备

**功能列表：**

- 设备列表（在线状态、版本信息）
- 设备注册/解绑
- 远程配置下发
- 设备固件升级
- 设备运行监控
- 异常告警管理

### 报表统计 — 🟡 重要（P1）

多维度数据报表

**功能列表：**

- 销售日报/周报/月报
- 门店销售对比
- 商品销售排行
- 支付方式分布
- 客流统计
- 报表导出（Excel/PDF）

### 系统设置 — 🟡 重要（P1）

系统级配置管理

**功能列表：**

- 用户管理（增删改查）
- 角色管理
- 操作日志
- 系统参数配置
- 通知设置
- 数据字典

### CSV 数据管理 - Module — 🔴 核心（P0）

管理收银台软件模块配置，支持 JSON 格式的步骤规则配置

**功能列表：**

- Module 列表（搜索/分页/筛选）
- 新增/编辑/删除 Module
- JSON 规则在线编辑（步骤 steps 配置）
- 按 Location/Env 筛选
- Module 模板复制
- 批量导入/导出 CSV
- Module 版本管理
- 步骤路径校验（ps1 文件是否存在）

### CSV 数据管理 - Rule — 🔴 核心（P0）

管理设备匹配规则，支持 env（主机名匹配）和 group（设备型号匹配）两种类型

**功能列表：**

- Rule 列表（搜索/分页/筛选）
- 新增/编辑/删除 Rule
- 按 Type/Type/Location 筛选
- 规则正则校验
- 规则优先级排序
- 规则模拟测试（输入 HostName 看匹配哪个 Result）
- 批量导入/导出 CSV

### CSV 数据管理 - Store — 🔴 核心（P0）

管理门店网络与支付配置数据

**功能列表：**

- Store 列表（搜索/分页/筛选）
- 新增/编辑/删除 Store
- 按 Location/EFT 筛选
- IP 段格式校验
- 批量导入/导出 CSV
- 门店配置对比
- 网络配置可视化

### CSV 数据管理 - TillList — 🟡 重要（P1）

管理收银台设备清单（HostName 与 MAC 地址映射）

**功能列表：**

- TillList 列表（搜索/分页）
- 新增/编辑/删除 Till
- MAC 地址格式校验
- HostName 唯一性校验
- 批量导入/导出 CSV

### CSV 数据管理 - Var — 🟡 重要（P1）

管理环境变量配置，支持多环境（pr/pp/lab）多值

**功能列表：**

- Var 列表（搜索/分页/筛选）
- 新增/编辑/删除 Var
- 按 Var_Name/Env 筛选
- 同名变量多环境对比
- Matcher 规则配置
- 批量导入/导出 CSV

### CSV 文件生成与下载 — 🔴 核心（P0）

一键生成所有 CSV 配置文件并打包下载

**功能列表：**

- 手动触发 CSV 重新生成
- 选择指定 CSV 文件生成
- 生成记录与版本管理
- CSV 文件预览
- CSV 差异对比（Diff）
- 下载单文件 / 打包下载（ZIP）
- CSV 文件内容格式校验
- 生成历史与回滚

---

## 4. 后端 API 接口

### 认证服务 — 🔴 核心（P0）

用户认证与授权

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/auth/login | 登录 |
| POST | /api/auth/logout | 登出 |
| POST | /api/auth/refresh | 刷新令牌 |
| GET | /api/auth/permissions | 获取当前用户权限 |

### 门店服务 — 🔴 核心（P0）

门店 CRUD 与管理

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/stores | 门店列表 |
| POST | /api/stores | 新增门店 |
| PUT | /api/stores/:id | 编辑门店 |
| DELETE | /api/stores/:id | 删除门店 |
| PATCH | /api/stores/:id/status | 启停用门店 |
| GET | /api/stores/:id | 门店详情 |

### 设备服务 — 🔴 核心（P0）

设备注册、监控、配置下发

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/devices | 设备列表 |
| POST | /api/devices/register | 设备注册 |
| DELETE | /api/devices/:id | 解绑设备 |
| POST | /api/devices/:id/config | 下发配置 |
| GET | /api/devices/:id/status | 设备状态 |
| POST | /api/devices/:id/upgrade | 固件升级 |

### 系统管理服务 — 🟡 重要（P1）

用户、角色、日志管理

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/system/users | 用户列表 |
| POST | /api/system/users | 新增用户 |
| PUT | /api/system/users/:id | 编辑用户 |
| DELETE | /api/system/users/:id | 删除用户 |
| GET | /api/system/roles | 角色列表 |
| POST | /api/system/roles | 新增角色 |
| GET | /api/system/logs | 操作日志 |

### CSV Module 管理服务 — 🔴 核心（P0）

Module 数据的 CRUD 与 CSV 导入导出

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/csv/modules | Module 列表 |
| POST | /api/csv/modules | 新增 Module |
| PUT | /api/csv/modules/:id | 编辑 Module |
| DELETE | /api/csv/modules/:id | 删除 Module |
| POST | /api/csv/modules/import | 导入 CSV |
| GET | /api/csv/modules/export | 导出 CSV（Module.csv 格式） |
| PUT | /api/csv/modules/:id/rules | 编辑 JSON rules |

### CSV Rule 管理服务 — 🔴 核心（P0）

Rule 数据的 CRUD 与 CSV 导入导出

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/csv/rules | Rule 列表 |
| POST | /api/csv/rules | 新增 Rule |
| PUT | /api/csv/rules/:id | 编辑 Rule |
| DELETE | /api/csv/rules/:id | 删除 Rule |
| POST | /api/csv/rules/import | 导入 CSV |
| GET | /api/csv/rules/export | 导出 CSV（Rule.csv 格式） |
| POST | /api/csv/rules/test | 规则模拟测试 |

### CSV Store 管理服务 — 🔴 核心（P0）

Store 数据的 CRUD 与 CSV 导入导出

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/csv/stores | Store 列表 |
| POST | /api/csv/stores | 新增 Store |
| PUT | /api/csv/stores/:id | 编辑 Store |
| DELETE | /api/csv/stores/:id | 删除 Store |
| POST | /api/csv/stores/import | 导入 CSV |
| GET | /api/csv/stores/export | 导出 CSV（Store.csv 格式） |

### CSV TillList 管理服务 — 🟡 重要（P1）

TillList 数据的 CRUD 与 CSV 导入导出

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/csv/tills | TillList 列表 |
| POST | /api/csv/tills | 新增 Till |
| PUT | /api/csv/tills/:id | 编辑 Till |
| DELETE | /api/csv/tills/:id | 删除 Till |
| POST | /api/csv/tills/import | 导入 CSV |
| GET | /api/csv/tills/export | 导出 CSV（TillList.csv 格式） |

### CSV Var 管理服务 — 🔴 核心（P0）

Var 数据的 CRUD 与 CSV 导入导出

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/csv/vars | Var 列表 |
| POST | /api/csv/vars | 新增 Var |
| PUT | /api/csv/vars/:id | 编辑 Var |
| DELETE | /api/csv/vars/:id | 删除 Var |
| POST | /api/csv/vars/import | 导入 CSV |
| GET | /api/csv/vars/export | 导出 CSV（Var.csv 格式） |

### CSV 文件生成服务 — 🔴 核心（P0）

一键生成并下载所有 CSV 文件

**API 列表：**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/csv/generate | 触发重新生成所有 CSV |
| POST | /api/csv/generate/:type | 生成指定类型 CSV（module/rule/store/till/var） |
| GET | /api/csv/download/:type | 下载单个 CSV 文件 |
| GET | /api/csv/download/all | 打包下载全部 CSV（ZIP） |
| GET | /api/csv/diff/:type | CSV 版本差异对比 |
| GET | /api/csv/history/:type | CSV 生成历史 |

---

## 5. 数据库设计

| 表名 | 说明 | 主要字段 |
|---|---|---|
| store | 门店表 | id, name, code, address, contact, phone, status, created_at, updated_at |
| device | 设备表 | id, store_id, device_code, name, type, status, version, config, last_heartbeat, created_at |
| user | 用户表 | id, username, password_hash, name, role_id, store_id, status, created_at |
| role | 角色表 | id, order_id, product_id, quantity, price, subtotal |
| operation_log | 操作日志表 | id, user_id, action, target_type, target_id, detail(JSON), created_at |
| csv_module | CSV Module 数据表 | id, name, location, modules(JSON), env, created_at, updated_at |
| csv_rule | CSV Rule 数据表 | id, name, type, location, env_name, rule_pattern, result, sort, created_at, updated_at |
| csv_store | CSV Store 数据表 | id, store_number, network_segment, webpos_env, eft, location, rf_server, cashtill_seg_gw, created_at, updated_at |
| csv_till | CSV TillList 数据表 | id, hostname, mac_address, created_at, updated_at |
| csv_var | CSV Var 数据表 | id, var_name, value, env, matcher(JSON), created_at, updated_at |
| csv_generation_log | CSV 生成日志表 | id, generate_time, file_type, file_count, operator, status, detail |

---

## 6. 技术栈

### 6.1 前端技术栈

| 类别 | 选型 |
|---|---|
| 框架 | React 18 |
| UI 组件库 | Ant Design 5 |
| 构建工具 | Vite |
| 状态管理 | Zustand |
| HTTP 客户端 | Axios |
| CSV 处理库 | papaparse |
| 表单库 | React Hook Form |
| 其他 | react-router-dom 6, dayjs, @ant-design/icons |

### 6.2 后端技术栈

| 类别 | 选型 |
|---|---|
| 开发语言 | Go 1.22+ |
| HTTP 框架 | Gin |
| 路由 | gin-router |
| ORM | GORM |
| 数据库 | PostgreSQL 16 |
| 缓存 | Redis |
| 数据库迁移 | golang-migrate |
| 配置管理 | Viper |
| 认证与权限 | golang-jwt / casbin |
| CSV 处理 | encoding/csv (stdlib) |
| 参数校验 | go-playground/validator |
| API 文档 | swaggo/swag |
| 日志库 | zap |
| 测试框架 | testify |

### 6.3 部署运维技术栈

| 类别 | 选型 |
|---|---|
| 容器化 | Docker + Docker Compose |
| CI/CD | GitHub Actions |
| 服务器 | Linux (Ubuntu 22.04) |
| 反向代理 | Nginx |

---

## 7. 非功能性需求

### 性能要求

- 页面加载时间不超过 2 秒
- API 响应时间不超过 500ms（95分位）
- 支持 200+ 并发用户
- 报表查询不超过 5 秒

### 安全要求

- 使用 HTTPS
- JWT Token 鉴权
- 敏感数据加密存储
- 操作日志记录审计
- 防 SQL 注入/XSS/CSRF

### 可用性要求

- 系统可用性 ≥ 99.9%
- 支持配置自动备份
- 服务异常自动告警

### 可扩展性要求

- 支持水平扩展
- 支持多门店数据隔离
- 配置模板支持批量下发

---

## 8. 页面路由规划

| 路径 | 页面名称 | 组件 | 可访问角色 |
|---|---|---|---|
| /login | 登录 | Login.vue | 所有 |
| / | 仪表盘 | Dashboard.vue | 超级管理员、运营管理员 |
| /stores | 门店管理 | StoreList.vue | 超级管理员、运营管理员 |
| /stores/:id | 门店详情 | StoreDetail.vue | 超级管理员、运营管理员、门店店长 |
| /devices | 设备管理 | DeviceList.vue | 超级管理员、运营管理员 |
| /devices/:id | 设备详情 | DeviceDetail.vue | 超级管理员、运营管理员 |
| /layout | 收银台布局 | LayoutConfig.vue | 超级管理员、运营管理员 |
| /reports | 报表统计 | Dashboard.vue | 超级管理员、运营管理员、财务人员 |
| /system/users | 用户管理 | UserList.vue | 超级管理员 |
| /system/roles | 角色管理 | RoleList.vue | 超级管理员 |
| /system/logs | 操作日志 | OperationLog.vue | 超级管理员 |
| /system/settings | 系统设置 | SystemSettings.vue | 超级管理员 |
| /csv/modules | CSV Module 管理 | CsvModuleList.vue | 超级管理员、运营管理员 |
| /csv/modules/:id | Module 详情 | CsvModuleDetail.vue | 超级管理员、运营管理员 |
| /csv/rules | CSV Rule 管理 | CsvRuleList.vue | 超级管理员、运营管理员 |
| /csv/rules/:id | Rule 详情 | CsvRuleDetail.vue | 超级管理员、运营管理员 |
| /csv/stores | CSV Store 管理 | CsvStoreList.vue | 超级管理员、运营管理员 |
| /csv/tills | CSV TillList 管理 | CsvTillList.vue | 超级管理员、运营管理员 |
| /csv/vars | CSV Var 管理 | CsvVarList.vue | 超级管理员、运营管理员 |
| /csv/generate | CSV 文件生成 | CsvGenerate.vue | 超级管理员、运营管理员 |
| /csv/history | CSV 生成历史 | CsvHistory.vue | 超级管理员、运营管理员 |

---

## 9. 附加说明

> - 以上为初始版本规划，可根据实际需求调整
> - P0 为必须实现的核心功能，P1 为迭代优化功能
> - 所有配置变更需记录操作日志
> - API 统一返回格式: { code, message, data }
> - 建议分两期开发：第一期实现 P0 功能，第二期实现 P1 功能

---

*文档结束 — 生成时间：2026-05-27 10:53:23*
