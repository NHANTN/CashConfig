# 收银台配置管理平台 - 开发进度与待办

---

## ✅ 已完成 (P0 核心)

### 基础设施
- [x] Go 后端项目初始化 (Gin + GORM + PostgreSQL + Viper + Zap)
- [x] React 前端项目初始化 (Vite + Ant Design 5 + Zustand + Axios)
- [x] 后端路由、配置、数据库连接、自动迁移
- [x] 前端侧边导航菜单 + API 代理配置

### 认证与登录
- [x] 后端: User / Role 模型、JWT 认证中间件、登录/刷新/权限 API
- [x] 前端: 登录页、auth store (Zustand + persist)、路由守卫 (PrivateRoute)
- [x] 种子数据: 默认超级管理员 (admin / admin123) + 运营管理员角色

### CSV Module 管理
- [x] CRUD (列表/详情/新增/编辑/删除) + CSV 导出/导入
- [x] Modules 字段引用 Group 列表，导出时解析为完整 JSON

### CSV Rule 管理
- [x] CRUD + CSV 导出/导入
- [x] 规则模拟测试 (输入 HostName 匹配)
- [x] 优先级排序 (sort 字段)

### CSV Store 管理
- [x] CRUD + CSV 导出/导入

### CSV TillList 管理
- [x] CRUD + CSV 导出/导入

### CSV Var 管理
- [x] CRUD + CSV 导出/导入

### Group 管理
- [x] 独立 CRUD
- [x] Steps 字段使用脚本路径下拉选择 (扫描 win-till-modules/)

### CSV 文件生成与下载
- [x] 一键生成所有/指定类型 CSV 文件
- [x] 下载单个 CSV / 全部打包 ZIP
- [x] 生成历史记录
- [x] 版本差异对比 (Diff)

### 仪表盘
- [x] 首页数据概览 (各实体数量统计卡片)

---

## 📋 待办

### P0 - 门店管理 (独立业务模块)
需新建模型、API、前端页面

| 模块 | 后端 | 前端 |
|------|------|------|
| 门店列表 (搜索/分页/筛选) | `GET /api/business/stores` | StoreList (Business) |
| 新增/编辑门店 | `POST/PUT /api/business/stores` | StoreDetail (Business) |
| 门店启停用 | `PATCH /api/business/stores/:id/status` | 状态切换开关 |
| 门店详情 (设备数量、员工数) | `GET /api/business/stores/:id` | 详情页 |
| 门店分组管理 | `POST/DELETE /api/business/store-groups` | 分组管理 |

**模型字段建议**: id, name, code, address, contact, phone, status, group_id, created_at, updated_at

### P0 - 设备管理
需新建模型、API、前端页面

| 模块 | 后端 | 前端 |
|------|------|------|
| 设备列表 (在线状态、版本信息) | `GET /api/devices` | DeviceList |
| 设备注册/解绑 | `POST/DELETE /api/devices` | DeviceDetail |
| 远程配置下发 | `POST /api/devices/:id/config` | 配置推送按钮 |
| 设备固件升级 | `POST /api/devices/:id/upgrade` | 升级管理 |
| 设备运行监控 | `GET /api/devices/:id/status` | 状态面板 |
| 异常告警管理 | `GET/PUT /api/devices/alerts` | 告警列表 |

**模型字段建议**: id, store_id, device_code, name, type, status, version, config(JSON), last_heartbeat, created_at

### P1 - 报表统计
| 模块 | 后端 | 前端 |
|------|------|------|
| 销售日报/周报/月报 | `GET /api/reports/sales` | 报表页面 (ECharts/AntV) |
| 门店销售对比 | `GET /api/reports/store-compare` | 对比图表 |
| 商品销售排行 | `GET /api/reports/product-rank` | 排行榜 |
| 支付方式分布 | `GET /api/reports/payment-dist` | 饼图 |
| 客流统计 | `GET /api/reports/traffic` | 趋势图 |
| 报表导出 (Excel/PDF) | `GET /api/reports/export` | 导出按钮 |

### P1 - 系统设置
| 模块 | 后端 | 前端 |
|------|------|------|
| 用户管理 (增删改查) | `GET/POST/PUT/DELETE /api/system/users` | UserList / UserDetail |
| 角色管理 | `GET/POST/PUT /api/system/roles` | RoleList / RoleDetail |
| 操作日志 | `GET /api/system/logs` | LogList |
| 系统参数配置 | `GET/PUT /api/system/settings` | SettingsForm |
| 通知设置 | `GET/PUT /api/system/notifications` | NotificationSettings |
| 数据字典 | `GET/POST/PUT/DELETE /api/system/dict` | DictManager |

### P1 - Store 校验增强
- [ ] IP 段格式校验 (Network_Segment 字段)
- [ ] 门店配置对比功能

### P1 - TillList 校验增强
- [ ] MAC 地址格式校验
- [ ] HostName 唯一性校验

### 后续优化
- [ ] Swagger API 文档 (swaggo/swag)
- [ ] Docker + Docker Compose 容器化
- [ ] 单元测试 (testify)
- [ ] GitHub Actions CI/CD

---

## 技术栈

| 层 | 技术 |
|----|------|
| 前端 | React 18 + TypeScript + Ant Design 5 + Vite + Zustand + Axios |
| 后端 | Go 1.22+ + Gin + GORM + PostgreSQL 16 |
| 认证 | golang-jwt + bcrypt |
| 日志 | zap |
| CSV 处理 | encoding/csv (stdlib) + papaparse (前端) |

## 启动方式

```bash
# 后端
cd backend
go run cmd/server/main.go

# 前端
cd frontend
npm run dev

# 数据库
# 需要本地 PostgreSQL 16，数据库名: cashier_config
```

默认管理员: `admin` / `admin123`
